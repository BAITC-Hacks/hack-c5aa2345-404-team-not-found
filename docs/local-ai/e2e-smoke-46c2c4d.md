# Срочная проверка EXE → backend → Local AI

Дата: 23.09.2026, компьютер Local AI, часовой пояс UTC+05:00.

- Проверенный main: `46c2c4dd75450b538bfda01f585213023c78c673`.
- Код запущен в отдельном detached worktree `Hakaton-backend-run`;
  ветка Local AI и исходная работа сохранены.
- Команда: `./scripts/start-backend.ps1 -VenvPath 'C:\Users\Amankos\Downloads\Hakaton\.venv'`.
- Один Uvicorn worker, `127.0.0.1:8000`; локальная Ollama оставлена работающей.
- Окружение, зависимости, модели и драйверы не устанавливались и не обновлялись.
- Health: `status=ok`, `local_only=true`; ffmpeg/stt/diarization/analysis/export — `ready`.
- EXE собран `desktop/build-native.ps1`, SHA-256:
  `618d3b57f33eb6727b7b092b940abe7676ac2378db53c65f40e64dce4c840906`.

## Фактический результат

Планировался существующий короткий фрагмент 90 секунд, однако при проверке окна
EXE уже обрабатывал полную согласованную запись 274.25 с. Обработка не прерывалась,
дубликат задания не создавался. Последовательно наблюдались `transcribing`,
`diarizing`, `analyzing`, затем **`completed`**, `error=null`.

Задача создана в 17:54:11.517837; к 17:57:34 уже получен результат и скачаны оба
экспорта. Это верхняя граница около 203 секунд, включающая наблюдение и сохранение,
а не точное измерение времени моделей. Отдельные длительности этапов не измерялись.

- Результат backend: 48 сегментов, 4 анонимные метки, 6 поручений.
- Все 6 авторов поручений — `null`; личности не выдумывались.
- EXE показал «Обработка завершена», «Готово» и непустое поле результата;
  кнопки DOCX/PDF стали доступны. Демо не использовалось.
- Backend создал оба экспорта; они дополнительно скачаны через локальный
  `/api/meetings/{id}/export?format=...` вне Git. Диалоги сохранения EXE отдельно
  не проверялись: приоритетом было получить и сохранить один результат.
- DOCX: 42 010 байт. PDF: 100 486 байт.
- Локальные файлы: `%LOCALAPPDATA%/HackAlemAI/results/e2e-46c2c4d-30d07338/`
  — `SAMRUK-Protocol.docx` и `SAMRUK-Protocol.pdf`.

## Ограничения и передача

Повторные тесты и улучшения намеренно отложены. Качество слов, RU/KK, назначения
поручений, временных границ и меток людей вручную не проверялось; WER/DER не измерены.
DOCX/PDF получены непустыми, но визуальная проверка в Word/PDF-viewer не выполнена.
Предупреждения pyannote об отключённом TF32 и коротком pooling не остановили задачу.
89.62% из прежнего сравнения с PDF не являются точностью этого запуска.

Backend и Ollama оставлены работающими для демонстрации. Medet может проверить
один успешный сценарий в уже открытом EXE и затем провести визуальную/содержательную
приёмку экспорта. Записи, расшифровки, результаты и файлы протокола не включены в Git.
