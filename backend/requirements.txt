# Planned dependencies. No package or AI model has been installed at this stage.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.8,<3.0
python-multipart>=0.0.9,<1.0

# Planned local exports
python-docx>=1.1,<2.0
reportlab>=4.0,<5.0

# Planned local-only AI pipeline; install and test later on approved hardware.
faster-whisper>=1.1,<2.0
pyannote.audio>=3.3,<4.0
