# Planned dependencies; validated versions will be recorded after role-specific setup.
-r requirements-api.txt

# Planned local exports
python-docx>=1.1,<2.0
reportlab>=4.0,<5.0

# Planned local-only AI pipeline; install and test later on approved hardware.
faster-whisper>=1.1,<2.0
pyannote.audio>=4.0,<5.0
